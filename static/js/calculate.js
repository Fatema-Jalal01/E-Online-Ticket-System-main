



/*

const fare = JSON.parse(document.getElementById('bus_fare').textContent);
const numSeats = JSON.parse(document.getElementById('num_seats').textContent);


//

document.addEventListener('DOMContentLoaded', function() {
    const calculateBtn = document.getElementById('calculate-btn');
    calculateBtn.addEventListener('click', function() {
      const fare = document.getElementById('total-fare').textContent;
      const numSeats = document.getElementById('num-seats').textContent;
      const totalFare = fare * numSeats;
      document.getElementById('total-fare-display').textContent = totalFare;
    });
  });
  */